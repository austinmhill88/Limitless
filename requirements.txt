# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# WebSocket support
websockets>=12.0

# HTTP client
requests>=2.31.0

# Data processing
pandas>=2.1.0

# Environment variables
python-dotenv>=1.0.0

# Time zones (Python 3.9+ has zoneinfo built-in, but backport for older versions)
# tzdata is needed on Windows and for systems without timezone data
tzdata>=2023.3
